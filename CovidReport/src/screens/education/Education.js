import React from 'react';

import {CustomWebView} from '../../components';

import {EDUCATION_URL} from '../constant';

function Education() {
  return <CustomWebView url={EDUCATION_URL} />;
}
export default Education;
